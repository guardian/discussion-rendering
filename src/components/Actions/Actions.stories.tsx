import React from "react";
import { Actions } from "./Actions";

export default { component: Actions, title: "Actions" };

export const Default = () => <Actions />;
Default.story = { name: "default" };

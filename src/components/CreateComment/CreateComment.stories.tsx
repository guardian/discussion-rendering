import React from "react";
import { CreateComment } from "./CreateComment";

export default { component: CreateComment, title: "CreateComment" };

export const Default = () => <CreateComment />;
Default.story = { name: "default" };

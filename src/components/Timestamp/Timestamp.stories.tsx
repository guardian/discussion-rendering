import React from "react";
import { Timestamp } from "./Timestamp";

export default { component: Timestamp, title: "Timestamp" };

export const Default = () => <Timestamp />;
Default.story = { name: "default" };

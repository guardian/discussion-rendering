import React from "react";
import { Formatting } from "./Formatting";

export default { component: Formatting, title: "Formatting" };

export const Default = () => <Formatting />;
Default.story = { name: "default" };
